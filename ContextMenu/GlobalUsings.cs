// Project-wide alias for the editor's strong icon type: Icon is the IconPacks Lucide kind enum. Using it as
// the icon type means an unknown glyph is a compile error, not a silent runtime miss. Mirrors the alias the
// other icon-using projects carry.
global using Icon = IconPacks.Avalonia.Lucide.PackIconLucideKind;
