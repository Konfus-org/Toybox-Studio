// Project-wide alias for the editor's strong icon type: Icon is the IconPacks Lucide kind enum (1600+ icons;
// Icon.None means "no icon"). Using it as the icon type means an unknown glyph is a compile error, not a
// silent runtime miss. Projects that use icons carry their own copy of this alias.
global using Icon = IconPacks.Avalonia.Lucide.PackIconLucideKind;
