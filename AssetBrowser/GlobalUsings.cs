// The editor's strong icon type, project-wide (see Resources/GlobalUsings.cs) — Icon is the IconPacks
// Lucide kind enum, so tiles and the create menu name icons the same way the rest of the editor does.
global using Icon = IconPacks.Avalonia.Lucide.PackIconLucideKind;
