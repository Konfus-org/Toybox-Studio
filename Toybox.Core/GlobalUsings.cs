// Project-wide alias for the editor's strong icon type (see the app's GlobalUsings for the full rationale):
// Icon is the IconPacks Lucide kind enum, so an unknown glyph is a compile error, not a silent runtime miss.
global using Icon = IconPacks.Avalonia.Lucide.PackIconLucideKind;
