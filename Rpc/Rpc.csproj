<Project Sdk="Microsoft.NET.Sdk">

  <!-- The generic JSON-RPC transport (loopback TCP via StreamJsonRpc). Peer-agnostic:
       it knows nothing about Toybox methods, only how to connect, invoke, and notify. -->

  <ItemGroup>
    <ProjectReference Include="..\Utils\Utils.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="StreamJsonRpc" />
    <PackageReference Include="MessagePack" />
    <PackageReference Include="Newtonsoft.Json" />
  </ItemGroup>

</Project>
