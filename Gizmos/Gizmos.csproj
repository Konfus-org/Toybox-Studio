<Project Sdk="Microsoft.NET.Sdk">

  <!-- The editor-authored gizmo overlay: retained named layers of drawing ops (GizmoLayer, an
       engine-mirrored object on the EngineSync codegen) that the engine replays into its gizmo
       renderer over editor viewports. Gizmo is the facade call sites draw through. -->

  <ItemGroup>
    <ProjectReference Include="..\Utils\Utils.csproj" />
    <ProjectReference Include="..\AppHosting\AppHosting.csproj" />
    <ProjectReference Include="..\EngineApi\EngineApi.csproj" />
    <ProjectReference Include="..\Logging\Logging.csproj" />
    <ProjectReference Include="..\Generators\Generators.csproj"
                      OutputItemType="Analyzer"
                      ReferenceOutputAssembly="false" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" />
  </ItemGroup>

</Project>
