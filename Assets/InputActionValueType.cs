namespace Toybox.Studio.Assets;

/// <summary>The value shape an input action produces, mirroring the engine's
/// <c>InputActionValueType</c> value-for-value.</summary>
public enum InputActionValueType
{
    Button = 0,
    Axis = 1,
    Vector2 = 2,
}
